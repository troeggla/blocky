$u = 5

while $u > 0 do
    put "Value: "
    puts $u

    if $a.nil? do
        puts "a is nil"

        global $a = 12
        puts "not anymore..."
    end

    $u = $u - 1
end

scope.dump
puts

$u = nil
scope.dump

pen.goto 320, 240
8.times do
    pen.line 100
    pen.turn 45

    pen.goto 320, 240
    global $x = 123.456
end
pen.update

scope.dumpall
